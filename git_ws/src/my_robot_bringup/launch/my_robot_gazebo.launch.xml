<launch>
  <!-- Define variables -->
  <let name="urdf_path" 
       value="$(find-pkg-share my_robot_description)/urdf/my_robot.urdf.xacro"/>

  <let name="rviz_config_path"
       value="$(find-pkg-share my_robot_description)/rviz2/my_robot_urdf.rviz"/>

  <let name="gazebo_config_path"
       value="$(find-pkg-share my_robot_bringup)/config/gazebo_bridge.yaml"/>

  <let name="world_config_path"
       value="$(find-pkg-share my_robot_bringup)/world/robot_project.sdf"/>

  <!-- Robot State Publisher -->
  <node pkg="robot_state_publisher" exec="robot_state_publisher">
    <param name="robot_description" 
           value="$(command 'xacro $(var urdf_path)')"/>
  </node>

  <!-- Gazebo Simulation -->
  <include file="$(find-pkg-share ros_gz_sim)/launch/gz_sim.launch.py">
    <!-- <arg name="gz_args" value="empty.sdf -r"/> -->
    <arg name="gz_args" value="$(var world_config_path) -r"/>
  </include>

  <node pkg="joint_state_publisher_gui" exec="joint_state_publisher_gui"/>

  <!-- Spawn robot in Gazebo -->
  <node pkg="ros_gz_sim" exec="create" 
        args="-topic robot_description -name my_robot"/>

  <node pkg="ros_gz_bridge" exec="parameter_bridge">
    <param name="config_file" value="$(var gazebo_config_path)"/>
  </node>

  <!-- RViz -->
  <node pkg="rviz2" exec="rviz2" output="screen" 
        args="-d $(var rviz_config_path)"/>
</launch>
